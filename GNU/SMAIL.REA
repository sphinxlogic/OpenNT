Smail is a mail delivery agent that is somewhat compatible at the
user-level with sendmail.  Its configuration method is simpler but slightly
less flexible.

It is available via anonymous ftp from ftp.uu.net in /networking/mail/smail.
